[pytest]
testpaths = tests
pythonpath = .
markers =
    unit: fast unit tests, no external dependencies
    integration: integration tests with mocked APIs
    live: tests requiring real API keys - run manually only
addopts = -v --tb=short -m "unit or integration"
